apiVersion: v1
kind: Pod
metadata:
  name: resource-limits
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
       cpu: "250m"
       memory: "50Mi"
      limits:
       cpu: "500m"
       memory: "100Mi"
